[package]
name = "scx-synse-ipc"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
rkyv = { workspace = true }
# Only the async framing helpers need tokio I/O traits.
tokio = { version = "1", default-features = false, features = ["io-util"] }
